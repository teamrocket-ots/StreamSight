# Streamlit for interactive web apps
streamlit>=1.30.0

# Data manipulation and analysis
pandas>=2.0.0
numpy>=1.24.0

# Packet capture and analysis.
# Requires Wireshark's `tshark` to be installed AND on PATH.
pyshark>=0.6

# Interactive plotting and visualizations
plotly>=5.0.0

# Required on Windows so PyShark's event loop can run inside Streamlit
nest-asyncio==1.6.0

# Testing
pytest>=7.0.0
